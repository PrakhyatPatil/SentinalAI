rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /incidents/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /meta/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
